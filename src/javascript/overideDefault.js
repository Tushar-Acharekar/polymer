$(document).ready(function(){	

	adjustment();

	$(window).resize(function() {adjustment();});
});


function adjustment(){
/* 	var finalHeight = $(".main_header").position().top + $(".main_header").outerHeight(); 
	var screenWidth = screen.width;
 	$(".mainBody").css({"position": "relative", "top":finalHeight}); 
	$(".mdl-layout__drawer").css({"position": "relative", "top":finalHeight}); */
}

